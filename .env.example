# =============================================================================
# 环境变量示例
# =============================================================================
# 当前为极简空壳版：不依赖任何外部服务，不需要配置任何环境变量即可
# 本地开发（npm run dev）与构建（npm run build）。
#
# 部署到 EdgeOne Pages / 任何静态托管时同样无需环境变量。
# 如未来需要恢复可选功能（评论、AI 助手等），再按对应功能的文档补配即可。
#
# 开发小工具：文章页「Edit」按钮（打开本地编辑器）在 config/site.yaml 的
# dev 段配置，与 .env 无关。
