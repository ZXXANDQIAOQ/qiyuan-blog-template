# =============================================================================
# 环境变量示例
# =============================================================================
# 基础站点（纯静态博客本身）不依赖任何环境变量，
# 不需要配置即可本地开发（npm run dev）与构建（npm run build）。
# =============================================================================

# =============================================================================
# Studio 内容后台（可选功能）
# =============================================================================
# 后台让你能在浏览器里登录并在线编辑文章，不必每次改文件再推送 GitHub。
#
# 这些变量**不要**写进仓库。生产环境请在
#   EdgeOne 控制台 → 项目 → 设置 → 环境变量
# 里配置；本地开发想联调时可复制本文件为 .env（.env 已被 .gitignore 忽略）。
#
# 先跑一次下面这个命令生成口令哈希与会话密钥：
#   node scripts/studio-hash-password.mjs '设一个足够长的密码'
# =============================================================================

# 管理员账号
# STUDIO_USERNAME=admin

# 口令哈希，格式 pbkdf2$<迭代次数>$<salt>$<hash>
# 由 scripts/studio-hash-password.mjs 生成，请勿填入明文密码
# STUDIO_PASSWORD_HASH=pbkdf2$100000$...

# 会话签名密钥，随便一串长随机字符（同样由上面的命令生成）
# STUDIO_SESSION_SECRET=...

# 点后台「发布到网站」时触发的部署钩子 URL
# 在 EdgeOne 控制台：项目 → 设置 → 构建与部署 → 部署触发器 → 新建
# 未配置时发布按钮会提示未配置，但保存文章本身不受影响
# EDGEONE_DEPLOY_HOOK=https://...

# ---- 构建期拉取文章（Node 侧脚本使用） ----
# 后台把文章存在 EdgeOne Blob 里，构建前需要把它们同步到 src/content/blog/。
# 未配置这两项时 scripts/studio-pull-content.mjs 会静默跳过，
# 已提交到仓库的文章照常构建，不影响现有流程。
# STUDIO_BLOB_PROJECT_ID=makers-xxxxx
# STUDIO_BLOB_TOKEN=xxxxx

# Blob store 名称，与 EdgeOne 控制台创建的一致，默认 studio-content
# STUDIO_BLOB_NAME=studio-content


# =============================================================================
# 开发小工具
# =============================================================================
# 文章页「编辑」按钮（打开本地编辑器）在 config/site.yaml 的 dev 段配置，
# 与 .env 无关。
